version: '2'

services:
  main:
    build: .
    restart: unless-stopped
    privileged: true
    environment:
      - EDITOR=vim
    volumes:
      - ../StackOverflow:/home/StackOverflow
    networks:
        default:

networks:
    default:

