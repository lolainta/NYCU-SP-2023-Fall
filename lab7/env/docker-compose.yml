version: '2'

services:
  main:
    build: .
    restart: unless-stopped
    privileged: true
    environment:
      - EDITOR=vim
    volumes:
      - ../StackOverflow:/home/StackOverflow
      - ../ShellCode:/home/ShellCode
      - ../GOT:/home/GOT
    networks:
        default:

networks:
    default:

